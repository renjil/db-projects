-- Juice Shop Genie Demo: Create Schema
-- This script creates the schema for the Juice Shop demo

CREATE SCHEMA IF NOT EXISTS ${catalog}.${schema}
COMMENT 'Juice Shop demo for Genie Space - sample retail analytics data';
